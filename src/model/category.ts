import { Timestamp } from 'rxjs';

export class Category {
    categoryId: string;
    categoryName: string;
    description: string;
    createdAt: number;
    updatedAt: number;
    deletedAt: number;
    status: string;
}